version: "3.2"

services:
  dev:
    container_name: charger-node
    image: paritytech/ci-linux:974ba3ac-20201006
    working_dir: /var/www/charger-node
    ports:
      - "9944:9944"
    environment:
      - CARGO_HOME=/var/www/charger-node/.cargo
    volumes:
      - .:/var/www/charger-node
      - type: bind
        source: ./.local
        target: /root/.local
    command: bash -c "cargo build --release && ./target/release/charger-node --dev --ws-external"